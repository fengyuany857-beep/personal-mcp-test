{
  "name": "personal-mcp-test",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "node --test --experimental-strip-types tests/research.test.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.23.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
